<% @schedules.each_with_index do |schedule, index| %>
<tr>
  <% if schedule.status == "New" %>
    <td><%= check_box_tag "schedule_ids[]", schedule.id, false, class: 'selectable',name: 'checkbox' %></td>
    <% else %>
    <td></td>
  <% end %>
  <td><%= index + 1 %></td>
  <td><%= schedule.desc %></td>
  <td><%= schedule.company.name %></td>
  <td><%= schedule.period.from_date.strftime('%b %d, %Y') %> - <%= schedule.period.to_date.strftime('%b %d, %Y') %></td>
  <td><%= schedule.start_date.strftime('%b %d, %Y') %></td>
  <td><%= schedule.end_date_hr.strftime('%b %d, %Y') %></td>
  <td><%= schedule.status %></td>
  <td>
    <% if schedule.status == "In-progress" %>
      <%= link_to(('<i class="fa fa-pencil icon" style="color:#fc9803"></i>').html_safe, "javascript:void(0)", { class: "action_icon edit_btn", :"data-schedule" => schedule.id }) %>
    <% end %>
    <% if schedule.status == "New" %>
      <%= link_to(('<i class="fa fa-pencil icon" style="color:#fc9803"></i>').html_safe, "javascript:void(0)", { class: "action_icon edit_btn", :"data-schedule" => schedule.id }) %>
      <%= link_to(('<i class="fa fa-trash icon" style="color:red"></i>').html_safe, "javascript:void(0)", { class: "action_icon del_btn", :"data-schedule" => schedule.id }) %>
    <% end %>
  </td>
</tr>
<% end %>