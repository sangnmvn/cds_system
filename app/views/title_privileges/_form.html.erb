<%= form_with(model: title_privilege, local: true) do |form| %>
  <% if title_privilege.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(title_privilege.errors.count, "error") %> prohibited this title_privilege from being saved:</h2>

      <ul>
        <% title_privilege.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Name %>
    <%= form.text_field :Name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
