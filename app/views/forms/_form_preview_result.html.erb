<div class="title-table-form-preview-result">CDS Assessment Point</div>
<div style="overflow-x:auto;">
  <div class="horizontal-scroll-except-first-column">
    <table class="table table-striped table-primary table-responsive-sm table-preview-result">
      <thead>
        <tr>
          <th style="width:400px">Competency</th>
          <th style="width:60px" class="form-pr-rank">Rank</th>
          <th style="width:60px"class="form-pr-level">Level</th>
          <% @slots.each do |slot| %>
            <th class="slot-header lv<%= slot.to_i %>"><%= slot %></th>
          <% end %>
        </tr>
      </thead>
      <tbody>
        <% @competencies.each do |competency| %>
          <tr>
            <td style="width:400px" class="form-competency-name"><%= competency.name %></td>
            <td style="width:60px;" class="form-pr-rank type-number"><%= @hash_rank[competency.id][:value] %></td>
            <td style="width:60px"class="form-pr-level"><%= @hash_level[competency.id] %></td>
            <% @slots.each do |slot| %>
              <td class="slot <%= @result[competency.name][slot][:type] + " " + @result[competency.name][slot][:class] unless @result[competency.name][slot].nil? %>"><%= @result[competency.name][slot].nil? ? "" : @result[competency.name][slot][:value] %></td>
            <% end %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
