<%= binding.pry %>
$('.table-user-management').html(<%= render partial: "admin_users", locals: { admin_users: @admin_users, roles: @roles, projects: @projects, project_members: @project_members, companies: @companies } %>);
